apiVersion: v1
kind: Service

metadata:
  name: ml-engine-service
  namespace: shopfusion

spec:
  selector:
    app: ml-engine

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: ClusterIP