apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config
  namespace: shopfusion

data:
  NODE_ENV: "development"
  PORT: "5000"

  MIN_SUPPORT: "0.01"
  MIN_CONFIDENCE: "0.3"
  MIN_LIFT: "1.0"

  USE_PYTHON_ENGINE: "false"

  PYTHON_API_URL: "http://ml-engine-service:8000"

  CLIENT_URL: "http://frontend-service"